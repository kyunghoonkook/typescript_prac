declare const mabe: unknown;

if (mabe === 2) {
  const aNumber: number = mabe;
}

if (mabe === true) {
  const aBoolean: boolean = mabe;

  //   const aString: string = mabe;
}

if (typeof mabe === "string") {
  const aString: string = mabe;

  //  const aBoolean: boolean = mabe;
}
